{
  "name": "@naetia/agent-definitions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "build:dependencies": "pnpm --filter @naetia/assembly-domain build",
    "lint": "tsc --noEmit",
    "test": "pnpm run build:dependencies && vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@naetia/assembly-domain": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
