# The default fake adapter is deterministic and requires no secret.
ASSEMBLY_PORT=4317
MODEL_ADAPTER=fake
FAKE_MODEL_DELAY_MS=200

# Reserved for a later real-model orbit. Never expose provider keys to Vite.
# OPENAI_API_KEY=
