{
  "name": "@naetia/council-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build:dependencies": "pnpm --filter @naetia/assembly-domain --filter @naetia/assembly-protocol --filter @naetia/agent-definitions build",
    "dev": "pnpm run build:dependencies && vite --host 127.0.0.1 --port 5173",
    "build": "pnpm run build:dependencies && tsc --noEmit -p tsconfig.json && vite build",
    "lint": "eslint . --max-warnings=0",
    "test": "pnpm run build:dependencies && vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json --pretty false"
  },
  "dependencies": {
    "@naetia/agent-definitions": "workspace:*",
    "@naetia/assembly-domain": "workspace:*",
    "@naetia/assembly-protocol": "workspace:*",
    "@tanstack/react-query": "^5.90.10",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.2.0",
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "@vitejs/plugin-react": "^5.1.1",
    "jsdom": "^26.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.2.4",
    "vitest": "^4.0.8"
  }
}
